# Upstream sources, cloned for reference — each carries its own .git, so they are
# kept out of this repo rather than vendored. See README for how to restore them.
# (qutecipy is the exception: it is a real dependency, so it *is* vendored — see
# qutecipy/VENDORED.md.)
Gross-Pitaevskii-TCI/
reference_julia/

# Generated
opcache/
__pycache__/
*.pyc
Runs/
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc
docs/*.pdf
